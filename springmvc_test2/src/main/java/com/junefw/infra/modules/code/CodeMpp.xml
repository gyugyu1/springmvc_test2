<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.junefw.infra.modules.code.CodeMpp"> <!-- 지금파일지칭 -->

	<resultMap id="resultMapObj" type="com.junefw.infra.modules.code.Code"></resultMap> <!-- 결과객체지칭 -->
	<!-- infrCodeGroup -->
	<select id="selectList" resultMap="resultMapObj">
		select
			a.ifcgSeq
			,a.ifcgName
			,a.ifcgDelNy
		FROM 
			infrCodeGroup as a
		WHERE 1=1
		<choose>
			<when test="shIifcgDelNy==1">AND ifcgDelNy =1</when>
			<when test="shIifcgDelNy==0">AND ifcgDelNy =0</when>
		</choose>
			
			<if test="shIfcgName != null and !shIfcgName.equals('')"> AND ifcgName LIKE concat('%',#{shIfcgName},'%') </if>
			<!-- <if test="shIifgSeq != null and !shIifgSeq.equals('')"> AND ifcgSeq =#{shIifgSeq} </if> -->
			
	</select>

	<insert id="insert">
		INSERT INTO infrCodeGroup(
		ifcgName
		,ifcgDelNy
		) VALUES (
		#{ifcgName}
		,0
		)
		
	</insert>
	
		<select id="selectOne" resultMap="resultMapObj">
		SELECT
			a.ifcgSeq
		    , a.ifcgName
		    , a.ifcgDelNy
		FROM
			infrCodeGroup a
		WHERE 1=1
			and ifcgSeq = #{ifcgSeq}
	</select>
	
	<update id="update">
		UPDATE infrCodeGroup SET 
			ifcgName= #{ifcgName}
		WHERE 1=1
			AND ifcgSeq =#{ifcgSeq}
	</update>
	
	<!-- infrCode  -->
	
	<select id="selectListCode" resultMap="resultMapObj">
		select
			a.ifcdSeq
			,a.ifcdName
			,a.ifcdDelNy
		FROM 
			infrCode a
		WHERE 1=1
			AND a.ifcdDelNy =0
			<if test="shIifgSeq != null and !shIifgSeq.equals('')"> AND ifcgSeq =#{shIifgSeq} </if>
	</select>

	<insert id="insertCode">
		INSERT INTO infrCode(
		ifcdName
		,ifcdDelNy
		,ifcgSeq
		) VALUES (
		#{ifcdName}
		,0
		,#{ifcgSeq}
		)
		
	</insert>
	
		<select id="selectOneCode" resultMap="resultMapObj">
		SELECT
			a.ifcdSeq
		    , a.ifcdName
		    , a.ifcdDelNy
		FROM
			infrCode a
		WHERE 1=1
			and a.ifcdSeq = #{ifcdSeq}
	</select>
	
	<update id="updateCode">
		UPDATE infrCode SET 
			ifcdName= #{ifcdName}
		WHERE 1=1
			AND ifcdSeq =#{ifcdSeq}
	</update>
</mapper>